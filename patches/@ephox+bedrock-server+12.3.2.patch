diff --git a/node_modules/@ephox/bedrock-server/lib/main/ts/bedrock/compiler/Webpack.js b/node_modules/@ephox/bedrock-server/lib/main/ts/bedrock/compiler/Webpack.js
index 377cfe7..7a6b550 100644
--- a/node_modules/@ephox/bedrock-server/lib/main/ts/bedrock/compiler/Webpack.js
+++ b/node_modules/@ephox/bedrock-server/lib/main/ts/bedrock/compiler/Webpack.js
@@ -43,7 +43,7 @@ const webpackSharedRules = webpackRemap.concat([
         enforce: 'pre'
     },
     {
-        test: /\.(html|htm|css|bower|hex|rtf|xml|yml)$/,
+        test: /\.(html|htm|css|bower|hex|rtf|xml|yml|svg)$/,
         type: 'asset/source'
     },
     {
@@ -56,10 +56,6 @@ const getWebPackConfigTs = (tsConfigFile, scratchFile, dest, coverage, manualMod
     const TsConfigPathsPlugin = require('tsconfig-paths-webpack-plugin');
     // eslint-disable-next-line @typescript-eslint/no-var-requires
     const ForkTsCheckerWebpackPlugin = require('fork-ts-checker-webpack-plugin');
-    const compilerOverrides = {
-        rootDir: '.',
-        declarationMap: false
-    };
     return {
         stats: 'none',
         entry: scratchFile,
@@ -100,7 +96,6 @@ const getWebPackConfigTs = (tsConfigFile, scratchFile, dest, coverage, manualMod
                                 configFile: tsConfigFile,
                                 transpileOnly: true,
                                 projectReferences: true,
-                                compilerOptions: compilerOverrides
                             }
                         }
                     ]
@@ -124,9 +119,6 @@ const getWebPackConfigTs = (tsConfigFile, scratchFile, dest, coverage, manualMod
                     memoryLimit: manualMode ? 4096 : 2048,
                     configFile: tsConfigFile,
                     build: true,
-                    configOverwrite: {
-                        compilerOptions: compilerOverrides
-                    }
                 }
             }),
             new webpack.WatchIgnorePlugin({
